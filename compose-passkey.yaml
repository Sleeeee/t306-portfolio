services:
  passkey:
    container_name: portfolio-passkey
    build:
      context: ./passkey
      dockerfile: Dockerfile.dev
    env_file:
      - .env.prisma
    ports:
      - "127.0.0.1:8080:8080"
